# three node (two workers) cluster config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30001 # for kind we have to ddo this extra step for so that our service can assess over a container
    hostPort: 30001
- role: worker
- role: worker